# Clavis - Hackathon Submission Status

## Submission Requirements
| Requirement | Status | Notes |
|-------------|--------|-------|
| Deployed project link | ❌ TODO | Deploy to Vercel |
| 3-min demo video | ❌ TODO | Record and upload to YouTube |
| Public code repo | ✅ Done | github.com/amgaikwad4588/nexus-ai-agent |
| Text description | ❌ TODO | Write Devpost submission text |

---

## Judging Criteria - Current Status

### 1. Security Model — ✅ Strong
- ✅ Step-up auth for write operations
- ✅ Centralized Risk Engine (EXECUTE/STEP_UP/REAUTH/BLOCK)
- ✅ DPoP with auto-rotation (24h)

### 2. User Control — ✅ Medium-Strong
- ✅ Approval workflow with step-up auth cards
- ✅ Per-tool permission toggles

### 3. Technical Execution — ✅ Strong
- ✅ Token Vault for GitHub, Google, Discord
- ✅ Persistent audit log

### 4. Insight Value — ✅ High
- ✅ Pain points documented
- ✅ Blog post for $250 bonus
- ✅ Localhost vs Vercel deployment gotchas (see below)

---

## Pain Point: Localhost vs Vercel Deployment

Everything works locally but breaks on Vercel. Three issues discovered:

### 1. Read-only Filesystem
Vercel serverless functions have a **read-only filesystem**. Any `fs.writeFileSync()` call crashes at runtime. Our audit log wrote to `data/audit-log.json` — every tool call crashed silently. Fix: in-memory fallback when `process.env.VERCEL` is detected.

### 2. Token Vault Client Mismatch
The Token Vault exchange **must use the same client app that issued the refresh token**. We used `AUTH0_M2M_CLIENT_ID` (M2M app) but the refresh token came from `AUTH0_CLIENT_ID` (regular web app). Locally both happened to work; on Vercel the exchange silently returned `undefined` with no error logged. Fix: use the regular web app credentials for `Auth0AI`.

### 3. Next.js Cookie Context Lost During Streaming
On Vercel, `auth0.getSession()` (which reads cookies via `next/headers`) **returns null when called inside async tool execution during `streamText()`**. Locally, Node.js maintains the `AsyncLocalStorage` context so cookies remain accessible throughout the request. On Vercel serverless, the context is lost by the time the AI model calls a tool. Fix: cache the refresh token at the start of the request handler before streaming begins.

---

## Services: 13 Tools (4 Services)

| Service | Auth | Tools | Status |
|---------|------|-------|--------|
| Google | Token Vault | 2 | ✅ |
| GitHub | Token Vault | 5 | ✅ |
| Slack | Bot Token | 3 | ✅ |
| Discord | Token Vault | 3 | ✅ |

---

## Implemented Features

### Security
- [x] DPoP (EC P-256, auto-rotation 24h)
- [x] Risk Engine (low/medium/high/unknown)
- [x] Step-up auth cards
- [x] Persistent audit log

### UI/UX
- [x] DotGrid background (free GSAP)
- [x] Animated icons
- [x] BlurText animations
- [x] Glow effects

---

## Remaining Tasks

### High Priority
- [ ] Deploy to Vercel
- [ ] Record 3-min demo video
- [ ] Write Devpost text description

### Bonus
- [ ] Blog Post (~250 words) — $250 prize

---

## Key Pitch

**Problem:** "What happens when your AI agent sends a Slack message you didn't approve?"

**Solution:** Clavis — risk-based authorization, step-up auth, re-auth for destructive actions, full audit trail.
